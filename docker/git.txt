https://github.com/ldest/SreCourseApi/pkgs/container/sre-course%2Fapi

root@ad50dba9521e:/app/Migrations# cat init.sql
create table if not exists public.cities
(
    id   bigserial,
    name varchar(255)
);

create table if not exists public.forecast
(
    id          bigserial,
    "cityId"    bigint,
    "dateTime"  bigint,
    temperature integer,
    summary     text
);



DOTNET_ENVIRONMENT=Development swagger 

docker run ghcr.io/ldest/sre-course/api:0e6cd26 -e DOTNET_ENVIRONMENT=Development -p 80:8000

PgConnection": "server=localhost; port=5432; database=weather; user id=admin; password=admin;
